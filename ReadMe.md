Understand webpack hot module replacement

###Set up 

with HTML webpack plugin
- `npm install`
- `npm start` - will open index.html in browser

with webpack dev server
- `npm install`
- `npm run serve` - will open http://localhost:9000
